name: Fly Deploy
on:
  push:
    branches:
      - main
env:
  FLY_API_TOKEN: TBnTC3hhzAu-pMcmkDnxtRe9KFgBvqKYrS6Iai0bePw
jobs:
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: "pnpm"
      - uses: pnpm/action-setup@v2.2.2
        with:
          version: latest
          run_install: true
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: pnpm nx deploy renderer-app
