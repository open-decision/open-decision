{
  "name": "@open-decision/interpreter",
  "version": "0.0.1",
  "main": "./index.ts",
  "module": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": {
      "import": "./index.ts",
      "require": "./index.ts",
      "types": "./index.ts"
    }
  },
  "publishConfig": {
    "main": "./lib/index.cjs",
    "module": "./lib/index.mjs",
    "typings": "./lib/index.d.ts",
    "exports": {
      ".": {
        "import": "./lib/index.mjs",
        "require": "./lib/index.cjs",
        "types": "./lib/index.d.ts"
      }
    }
  },
  "files": [
    "/lib",
    "/src"
  ],
  "description": "The foundational types classes shared by all parts of the open decision application.",
  "sideEffects": false,
  "scripts": {
    "build": "rm -rf lib & tsc --composite false --incremental false & vite build ",
    "prepublish": "pnpm build",
    "prettier": "prettier --check src/",
    "prettier:fix": "prettier --write src/",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "yarn lint --fix",
    "verify": "run-p prettier lint",
    "verify:fix": "yarn prettier:fix && yarn lint:fix"
  },
  "dependencies": {
    "@open-decision/type-classes": "workspace:^0.0.1"
  },
  "devDependencies": {
    "typescript": "^4.4.4",
    "vite": "^2.6.13"
  }
}
