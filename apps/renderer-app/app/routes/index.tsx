import { Heading, Stack } from "@open-decision/design-system";

export default function Index() {
  return (
    <Stack center css={{ height: "100%" }}>
      <Heading>Die url enthält keine Projekt id.</Heading>
    </Stack>
  );
}
