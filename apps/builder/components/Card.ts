import { Stack, styled } from "@open-decision/design-system";

export const Card = styled(Stack, {
  padding: "$5",
  border: "$border$layer",
  boxShadow: "$1",
  borderRadius: "$md",
  layer: "1",
  gap: "$4",
});
