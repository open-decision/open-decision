query ALL_TREES {
  allDecisionTrees {
    edges {
      node {
        id
        name
        tags
        createdAt
      }
    }
  }
}

query USER {
  me {
    email
    firstName
    username
  }
}
