import { styled } from "utils/stitches.config";

export const Input = styled("input", {
  borderRadius: "$md",
  width: "100%",
  paddingBlock: "$2",
  paddingInline: "$3",
  lineHeight: "$tight",
  backgroundColor: "$gray100",
  borderColor: "$gray300",
  borderWidth: "$2",
  boxShadow: "$inner",
});
